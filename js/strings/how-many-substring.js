import { lookupService } from "dns";

function countSubstrings(s, queries) {

	queries.array.forEach(elem => {
		let subStr = s.substring(elem[0], elem[1]);
		let substrLength = subStr.length;
		for(var i = 0 ; i < substrLength; i++){

		}
	});

}

function createSuffixArray(){
	
}

function lcp(){

}